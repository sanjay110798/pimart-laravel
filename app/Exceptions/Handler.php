<?php

namespace App\Exceptions;

use Illuminate\Foundation\Exceptions\Handler as ExceptionHandler;
use Throwable;

class Handler extends ExceptionHandler
{
    /**
     * A list of the exception types that are not reported.
     *
     * @var array
     */
    protected $dontReport = [
        //
    ];

    /**
     * A list of the inputs that are never flashed for validation exceptions.
     *
     * @var array
     */
    protected $dontFlash = [
        'password',
        'password_confirmation',
    ];

    /**
     * Report or log an exception.
     *
     * @param  \Throwable  $exception
     * @return void
     *
     * @throws \Exception
     */
    public function report(Throwable $exception)
    {
        parent::report($exception);
    }

    /**
     * Render an exception into an HTTP response.
     *
     * @param  \Illuminate\Http\Request  $request
     * @param  \Throwable  $exception
     * @return \Symfony\Component\HttpFoundation\Response
     *
     * @throws \Throwable
     */
    public function render($request, Throwable $exception)
    {
        if(strpos($request->url(), '/api/') !== false ){
            return $this->customApiResponse($exception);
        }elseif ($request->expectsJson()) {
            return $this->customApiResponse($exception);
        }else{
            
            return parent::render($request, $exception);
        }
        
    }
    
    private function customApiResponse($exception)
    {
        if (method_exists($exception, 'getStatusCode')) {
            $statusCode = $exception->getStatusCode();
        } else {
            $statusCode = 500;
        }

        $response = ['error' => true, 'message' => '', 'data' => []];

        switch ($statusCode) {
            case 401:
                $response['message'] = 'Unauthorized';
                break;
            case 403:
                $response['message'] = 'Forbidden';
                break;
            case 404:
                $response['message'] = 'Not Found';
                break;
            case 405:
                $response['message'] = 'Method Not Allowed';
                break;
            case 422:
                $response['message'] = $exception->original['message'];
                break;
            default:
                //$response['message'] = ($exception->getMessage()) ? $exception->getMessage() : 'Whoops, looks like something went wrong';
                $response['message'] = ($exception->getMessage()) ? $exception->getMessage(). ' on '.$exception->getFile().' in '.$exception->getLine() : 'Whoops, looks like something went wrong';
                break;
        }

        return response()->json($response);
    }
}
